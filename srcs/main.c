#include "../inc/miniRT.h"

int main()
{
	//t_mlx_info	*win;

	//ft_create_win(win);
	//remplir pixels
	//mlx_put_image_to_window(win->mlx_ptr, win->win_ptr, win->img, 0, 0);
	//////hook
	//mlx_loop(win->mlx_ptr);

	printf("this is love\n");
}
